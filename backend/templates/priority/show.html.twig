{% extends 'base.html.twig' %}

{% block title %}Priority{% endblock %}

{% block body %}
    <h1>Priority</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ priority.id }}</td>
            </tr>
            <tr>
                <th>Priority_name</th>
                <td>{{ priority.priorityName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_priority_index') }}">back to list</a>

    <a href="{{ path('app_priority_edit', {'id': priority.id}) }}">edit</a>

    {{ include('priority/_delete_form.html.twig') }}
{% endblock %}
