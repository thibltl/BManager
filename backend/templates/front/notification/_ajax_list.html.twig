{% if notifications is empty %}
    <div class="notif-empty">
        <p>Aucune notification pour le moment.</p>
    </div>
{% else %}
    <ul class="notif-list">
        {% for n in notifications %}
            <li class="notif-item">

                <div class="notif-header-line">
                    {% if n.type == 'task_assigned' %}
                        <span class="notif-type notif-type-assigned">📌 Tâche assignée</span>
                    {% elseif n.type == 'task_created' %}
                        <span class="notif-type notif-type-created">🆕 Tâche créée</span>
                    {% elseif n.type == 'task_updated' %}
                        <span class="notif-type notif-type-updated">✏️ Tâche modifiée</span>
                    {% else %}
                        <span class="notif-type">🔔 Notification</span>
                    {% endif %}

                    <small class="notif-date">
                        {{ n.createdAt|date('d/m/Y H:i') }}
                    </small>
                </div>

                <p class="notif-message">
                    {{ n.message }}
                </p>

                
            </li>
        {% endfor %}
    </ul>
{% endif %}
