{% extends 'base_front.html.twig' %}

{% block body %}
<!-- Bloc projets -->
<div class="dashboard-block mb-4">
    <h2 class="block-title">🗂️ Mes projets</h2>
    <div class="row">
        {% for project in projects %}
            <div class="col-md-4 mb-4">
                <div class="card project-card">
                    <div class="card-body">
                        <h5 class="card-title">{{ project.projectName }}</h5>
                        <p class="card-text">{{ project.projectDesc }}</p>
                        <a href="{{ path('front_project_show', {id: project.id}) }}" class="btn btn-sm btn-primary">Voir</a>
                    </div>
                </div>
            </div>
        {% else %}
            <p>Aucun projet enregistré.</p>
        {% endfor %}
    </div>
</div>

<!-- Bloc vertical des tâches façon sidebar -->
<div class="task-sidebar-block">
    <h2 class="block-title">✅ Mes Taches</h2>
    <ul class="nav flex-column task-nav">
        {% for task in tasks %}
            <li class="nav-item mb-2">
                <div class="nav-link task-item d-flex justify-content-between align-items-center">
                    <div>
                        <strong>{{ task.taskTitle }}</strong><br>
                        <small class="text-muted">Échéance : {{ task.taskDueDate|date('d/m/Y') }}</small>
                    </div>
                    <span class="badge bg-secondary">
                        {{ task.taskStatus ? task.taskStatus.statusName : 'Sans statut' }}
                    </span>
                </div>
            </li>
        {% else %}
            <li class="nav-item">Aucune tâche enregistrée.</li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
