{% extends 'base.html.twig' %}

{% block title %}Th{% endblock %}

{% block body %}
    <h1>Th</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ th.id }}</td>
            </tr>
            <tr>
                <th>Th_change_log</th>
                <td>{{ th.thChangeLog }}</td>
            </tr>
            <tr>
                <th>Th_updateat</th>
                <td>{{ th.thUpdateat ? th.thUpdateat|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_th_index') }}">back to list</a>

    <a href="{{ path('app_th_edit', {'id': th.id}) }}">edit</a>

    {{ include('th/_delete_form.html.twig') }}
{% endblock %}
